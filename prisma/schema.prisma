generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model configuracion {
  identificador String  @id
  valor         String?
  numerico      Boolean
}

model mundo {
  id String @id
}
